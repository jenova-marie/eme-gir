# Bulk data — large, regenerable from oracc.museum.upenn.edu
*.zip
*.sqlite*

# Logs
*.log

# Generated reports + runtime sentinels under data/
data/missing_corpora.json
data/.initialized

# Cached source download for build_cdli_db.py (~147 MB CSV from
# cdli-gh/data via media.githubusercontent.com). Regenerable; never
# checked in.
data/cdli_cat.csv

# Per-deployment env files — Auth0 tenant URLs, audiences, allowed
# hosts, and any client secrets. docker-compose auto-loads .env from
# the project root; never commit it.
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Editor / OS
.DS_Store
*.swp
.vscode/
.idea/

# Copyrighted reference material — kept locally, never committed
Sumerian Grammar (Edzard).pdf
*.pdf
